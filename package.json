{
  "name": "@toktokhan-dev/tok-builder-cli",
  "version": "0.3.2",
  "private": false,
  "description": "tok-builder CLI for pj-platform build-plan tracking (login/task/plan/run/phase/resume/init). Internal to toktokhan-dev.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/toktokhan-dev/tok-builder-cli.git"
  },
  "homepage": "https://github.com/toktokhan-dev/tok-builder-cli#readme",
  "type": "module",
  "bin": {
    "tokb": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=24"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "files": [
    "dist",
    "install.sh",
    "README.md"
  ]
}
